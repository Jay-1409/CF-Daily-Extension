rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The extension uses the authenticated server API. Only Firebase Admin writes Firestore.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
